var foo = "method("+argument1+","+argument2+")";
; f + s(single command in sequence can use . command in full repetition)
var foo = "method(" + argument1 + "," + argument2 + ")";

normal mode



shortcuts

f{char}
    find next occurence of {char}charater after cursor position.
;
    Filter lines through an external program.
